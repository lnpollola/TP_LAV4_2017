import { JugadorGanadoresPipe } from './jugador-ganadores.pipe';

describe('JugadorGanadoresPipe', () => {
  it('create an instance', () => {
    const pipe = new JugadorGanadoresPipe();
    expect(pipe).toBeTruthy();
  });
});
